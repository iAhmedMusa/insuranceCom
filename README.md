## Installation

```` cp .env.example .env ````

in .env file set your database credentials 

```` composer install ````

```` php artisan key:generate ````

```` php artisan migrate ````

```` php artisan serve ````
